import styled from 'styled-components'

export const Container = styled.div`
  aspect-ratio: 2 / 3;
  max-width: 25vw;
  padding: 2vw;
  align-items: center;
`

export const AvatarContainer = styled.div`
  aspect-ratio: 1 / 1;
  width: 60%;
  margin: 0 auto;
`

export const Rank = styled.b`
  font-size: 1.5em;
`

export const Name = styled.div`
  display: flex;
  gap: 0.8em;
`
